<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xcy.repository.TeamMapper">
    <select id="selectTop20" resultType="team">
        select * from team  order by hot desc limit 0 , 19
    </select>
    <select id="matchRecommend" resultType="team" >
        select * from team
    </select>
</mapper>